PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
# Use full LAPACK from rtools (R's Rlapack is a subset missing some routines)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) -llapack -lblas $(FLIBS)

PKG_CPPFLAGS = -UDEBUG -g -Wall -O2 -pedantic
